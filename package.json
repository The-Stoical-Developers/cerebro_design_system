{
  "name": "cerebro-design-system",
  "version": "1.0.0",
  "private": true,
  "description": "Cerebro design system monorepo — TypeScript tokens + React components",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/The-Stoical-Developers/cerebro_design_system.git"
  },
  "workspaces": [
    "build",
    "synapse/ts"
  ],
  "scripts": {
    "build": "npm run build --workspace=build",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "semantic-release": "^24.0.0",
    "@semantic-release/changelog": "^6.0.0",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.0"
  }
}
